info:
  name: notifications-test-push
  type: http
  seq: 99

http:
  method: POST
  url: "{{baseUrl}}/Admin/notifications/test-push"
  headers:
    - name: X-Admin-Token
      value: "{{adminToken}}"
  body:
    type: json
    data: "{\r

      \  \"token\": \"REPLACE_WITH_FCM_DEVICE_TOKEN\",\r

      \  \"title\": \"sportDeets test push\",\r

      \  \"body\": \"If you can read this, the pipeline works.\",\r

      \  \"data\": {\r

      \    \"deepLink\": \"sportdeets://picks\"\r

      \  }\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
