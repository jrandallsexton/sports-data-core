info:
  name: verify-outbox-used-teamSports
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrlProducer}}/api/test/outbox/teamsport/publish-via-service"
  headers:
    - name: X-Admin-Token
      value: "{{X-Admin-Token}}"
  body:
    type: json
    data: "{\r

      \  \"url\": \"http://foo.com\"\r

      }"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
