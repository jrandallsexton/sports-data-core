// jest-dom custom matchers (toBeInTheDocument, toHaveTextContent, …), wired to
// Vitest's expect. The /vitest entry point extends the right expect instance.
import "@testing-library/jest-dom/vitest";
