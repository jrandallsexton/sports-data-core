info:
  name: ai-refresh
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/admin/ai-refresh"
  headers:
    - name: X-Admin-Token
      value: "{{adminToken}}"
  body:
    type: json
    data: "{\r

      \  \"name\": \"Hello\",\r

      \  \"text\": \"Testing external connectivity from the internet, to the k8s cluster, over to Ollama.\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
