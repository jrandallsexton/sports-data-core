info:
  name: google-maps-geocode
  type: http
  seq: 10

http:
  method: GET
  url: https://maps.googleapis.com/maps/api/geocode/json?address=Torero+Stadium,+San+Diego,+CA+92111,+US&key={{googleApiKey}}
  params:
    - name: address
      value: Torero+Stadium,+San+Diego,+CA+92111,+US
      type: query
    - name: key
      value: {{googleApiKey}}
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
