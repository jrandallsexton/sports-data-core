info:
  name: publish-no-entity-changes-base
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrlProducer}}/api/test/outbox/teamsport/publish-no-entity-changes"
  headers:
    - name: X-Admin-Token
      value: "{{X-Admin-Token}}"
  body:
    type: json
    data: "{\r

      \  \"url\": \"http://foo.com\"\r

      }"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
