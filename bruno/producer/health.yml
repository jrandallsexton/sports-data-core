info:
  name: health
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrlProducer}}/api/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
