info:
  name: historical-sourcing
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrlProvider}}/api/sourcing/historical/seasons"
  body:
    type: json
    data: |-
      {
        "sport": 3,
        "sourceDataProvider": 0,
        "seasonYear": 2019
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
